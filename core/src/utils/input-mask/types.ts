
/**
 * The visibility of the mask.
 */
export type MaskVisibility = 'always' | 'focus' | 'never';

/**
 * The accepted format for the mask.
 */
export type MaskFormat = string | (string | RegExp)[] | boolean;
