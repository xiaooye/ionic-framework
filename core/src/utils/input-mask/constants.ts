import type { MaskVisibility } from "./types";

/**
 * The default placeholder character displayed in the input
 * when the mask is visible.
 */
export const defaultPlaceHolderChar = '_';

/**
 * The default mask visibility for the input.
 */
export const defaultMaskVisibility: MaskVisibility = 'always';
